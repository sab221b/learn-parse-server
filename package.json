{
  "name": "learn-parse-server",
  "version": "1.0.0",
  "description": "learn parse server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parse-server --appId myAppId --masterKey myMasterKey --serverURL 'http://localhost:1337/parse' --appName sabsParseDashboard",
    "dashboard": "parse-dashboard --appId myAppId --masterKey myMasterKey --serverURL 'http://localhost:1337/parse' --appName sabsParseDashboard"
  },
  "author": "Rajesh Ravichandran",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "parse-server": "^3.2.3"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "true"
  }
}
